{
  "name": "ghost-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile --target=bun-linux-x64 ./src/index.ts --outfile ../dist/ghost-agent"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0",
    "ulid": "^3.0.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
